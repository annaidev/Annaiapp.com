import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.annai.travelplanner",
  appName: "Annai Travel Planner",
  webDir: "dist/public",
};

export default config;
