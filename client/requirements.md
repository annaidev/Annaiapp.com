## Packages
date-fns | Formatting dates for trips
framer-motion | Smooth animations and page transitions

## Notes
The backend endpoints handle AI generations (POST /api/ai/*). These may take a few seconds to respond.
External links (Uber, Airbnb, etc.) are constructed purely on the frontend using the trip destination.
